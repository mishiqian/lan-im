# 局域网即时通信插件（lan-im）产品需求文档（PRD）

| 项 | 内容 |
|---|---|
| 产品 | lan-im（局域网即时通信） |
| 版本 | v1.0 草案 |
| 状态 | 评审中 |
| 基线 | v0.2.0（mDNS + WebSocket，群聊/私聊，本地落盘，悬浮窗） |
| 定位 | 局域网内安装了本插件的 DeepSeek Harness 实例之间的可靠沟通工具 |

---

## 1. 背景与目标

### 1.1 背景
多个 DeepSeek Harness 实例常部署在同一局域网（如同一团队的多台开发机/服务器）。运维和协作中，用户需要一个轻量、无需中心服务器、装完即用的通道来互相沟通。

### 1.2 目标
- **可靠**：消息不丢、可追溯（送达确认、离线补发）。
- **可感知**：关窗/切页也能第一时间知道来消息。
- **省心**：零配置发现，网络环境差异下有兜底。
- **安全可扩展**：从「开放信任」平滑演进到「共享密钥 + 可选加密」。
- **身份稳定**：以账号而非机器作为身份，人走到哪台机器，身份就跟到哪。

### 1.3 非目标（本期不做）
- 不做跨网段/公网中继（无中心服务器）。
- 不做中心化账号注册、好友关系链/陌生人社交（账号为局域网内本地声明）。
- 不做音视频通话。

---

## 2. 用户画像与场景

| 画像 | 场景 | 核心诉求 |
|---|---|---|
| 开发者/运维 | 给另一台机器上的自己或同事发一条「服务重启了」 | 快、可靠、不打扰 |
| 小团队 | 多台 Harness 之间共享状态、传配置文件 | 群聊 + 传文件 |
| 离线协作 | 对方机器当前关机，之后上线 | 离线消息能补发，不丢 |

---

## 3. 需求范围

**In**：账号体系、消息可靠性、通知/未读、发现兜底、文件传输、消息检索、安全、多频道、消息操作。

**Out**：中心化注册、好友关系链、公网中继、音视频、移动端。

---

## 4. 基线能力（v0.2.0）

- 发现：mDNS（`_lanim._tcp.local`）
- 消息：WebSocket 长连接（确定性发起方 + 断线重连 + 待发队列）
- 频道：公共频道群聊 + 一对一私聊
- 历史：本地 JSONL 落盘（上限 500 条）
- UI：侧边栏入口 + 悬浮可拖拽窗（✕/Esc/点击外关闭）+ 诊断日志
- 安全：开放信任、明文

---

## 5. 功能需求

### 5.1 消息可靠性（P0）

**用户故事**：我给一台当前离线机器的 Harness 发消息，它上线后能自动收到，且不重复。

**详细说明**
- **送达确认（ack）**：接收方成功落盘后回 `{t:'ack', id}`；发送方据此标记「已送达」。
- **离线补发**：
  - 每个节点维护**持久化 outbox**（按目标 nodeId 分队列，落盘）。
  - 发送时目标不在线 / 连接失败 → 消息入 outbox。
  - 对端重连成功后，按 `ts` 顺序补发 outbox 中的消息。
  - 收到对端 ack 后从 outbox 删除。
  - 依赖消息 `id` 全局唯一去重。
- **去重**：接收方按消息 `id` 幂等，重复投递只落盘一次。

**验收标准**
1. A 向离线的 B 发 3 条消息，B 上线后 30s 内全部收到，且顺序正确、无重复。
2. 每条消息在 A 端显示「已送达」状态。
3. outbox 有容量上限（如每目标 200 条），超出时最旧消息可被丢弃并提示。

---

### 5.2 通知与未读（P0）

**用户故事**：我切到别的页面干活时，有人给我发消息，我能马上知道。

**详细说明**
- **未读计数**：每个会话（公共频道 + 各 peer）维护未读数；悬浮按钮显示总未读角标；打开对应会话后清零。
- **桌面通知**：窗口关闭或页面不可见时，使用浏览器 `Notification` API（首次需用户授权）弹出通知，可选提示音。
- **免打扰**：可对单个节点 / 公共频道静音（静音会话不计入角标、不弹通知）。

**验收标准**
1. 窗口关闭时收到消息 → 悬浮按钮出现未读角标，数字正确。
2. 已授权时弹桌面通知；点击通知能唤起悬浮窗并定位到对应会话。
3. 静音的会话来消息不弹通知、不计未读。

---

### 5.3 发现机制兜底（P0）

**用户故事**：在组播被隔离（但 UDP 广播可达）的网络里，两台机器仍能互相发现。

**详细说明**
- 保留 mDNS；增加 **UDP 广播**（自定义报文，端口如 42123）作为第二通道。
- 两通道发现结果按 `nodeId` 合并去重；任一通道先发现即建立连接。
- 心跳超时统一由连接级心跳（已有 20s）判定。

**验收标准**
1. 禁用 mDNS（或模拟组播隔离）后，两节点仍能在 10s 内互相发现。
2. 双通道同时开启时，不产生重复节点、不产生重复连接。

---

### 5.4 文件/图片传输（P1）

**用户故事**：我直接拖一个配置文件或截图到聊天窗发给对方。

**详细说明**
- 通过既有 WebSocket 通道传输：元数据 `{t:'file', name, size, mime, sha256}` → 分片数据帧 → 收齐后校验。
- 支持图片即时预览（缩略图）+ 文件保存到本地接收目录。
- 单文件大小上限（建议 50MB），超过则提示。

**验收标准**
1. 发送并接收一个 20MB 文件，sha256 校验一致。
2. 图片在聊天窗内能预览，文件可点击打开所在目录。
3. 传输中有进度显示；中断可重传。

---

### 5.5 消息检索与历史（P1）

**用户故事**：我翻到三天前的消息，或搜索一个关键词。

**详细说明**
- 突破 500 条硬上限：历史按天分文件存储，或迁移到 SQLite。
- 全文搜索：按关键词 / 发送者 / 时间范围。
- 历史加载分页（向上滚动加载更早消息）。

**验收标准**
1. 历史消息条数不再被 500 截断（可配置保留天数/大小）。
2. 输入关键词能检索到匹配的历史消息。

---

### 5.6 安全（P2）

**用户故事**：一个陌生设备即使装了插件，也无法加入我们的聊天。

**详细说明**
- **共享密钥握手（PSK）**：节点需配置相同密钥才能互相发现/连接；握手做 HMAC 校验，不匹配则拒绝。
- **端到端加密（后置）**：ECDH 协商 + AES-GCM 加密消息体。

**验收标准**
1. 密钥不一致的两节点互相不可见、不可连接。
2. 密钥一致时体验与现在一致（无感）。

---

### 5.7 多频道（命名频道）（P2）

**用户故事**：我不想所有人在一个公共频道里刷屏，想按话题分频道。

**详细说明**
- 从单一公共频道升级为可创建/加入/离开的**命名频道**。
- 频道成员默认所有在线节点；后续可支持指定成员。
- 消息按频道隔离，未读按频道计数。

**验收标准**
1. 能创建、加入、离开频道；频道列表在各节点间同步。
2. 消息只出现在所属频道内。

---

### 5.8 消息操作（P2）

**用户故事**：我发错了，想撤回或改一下。

**详细说明**
- **撤回**：限时（如 2 分钟内），撤回后对端显示「对方撤回了一条消息」。
- **编辑**：修改后同步对端，显示「已编辑」。

**验收标准**
1. 撤回后对端消息消失/标记，且不能再次撤回。
2. 编辑后对端内容同步，且标注「已编辑」。

---

### 5.9 账号体系（P0）

**用户故事**：我有一个稳定账号（如 `alice`），不管在哪台机器的 Harness 上登录，别人联系的都是「我」这个账号，而不是「某台机器」。

**详细说明**
- **账号/节点分离**：账号是稳定的人类身份（username + 密码），节点是运行中的实例（设备）；一台节点登录一个账号。
- **身份模型**：消息的 `from`/`to` 由 `nodeId` 改为 `accountId`（username）；`nodeId` 降级为网络寻址/连接标识，不再对用户可见。
- **注册/登录（本地）**：本地创建账号（username + password，密码哈希落盘）；无中心服务器，账号即局域网内的本地声明，账号名唯一性靠先占 + 冲突提示。
- **多端（P2）**：同一账号可登录多台节点；发往该账号的消息路由到其全部在线节点。
- **鉴权边界**：账号密码只做本机登录校验；「这个账号是否可信」由 5.6 的 PSK 机制保证。

**验收标准**
1. 配置账号后，对端看到的发送者是账号显示名，而非机器名。
2. 重启后账号保持登录，无需重新输入。
3. 账号名冲突时给出明确提示，不静默覆盖。

---

## 6. 非功能需求

| 维度 | 要求 |
|---|---|
| 可靠性 | 消息至少一次投递 + 幂等去重；outbox 持久化，进程重启不丢 |
| 性能 | 单条消息端到端延迟 < 500ms（局域网内）；支持 ≥ 20 个在线节点 |
| 兼容性 | 与 v0.2.0 协议兼容或提供平滑升级（节点标识/历史格式不破坏） |
| 隐私 | 默认不向局域网外发任何数据；日志不含消息正文 |
| 健壮性 | 节点身份冲突不崩溃（当前 mDNS 服务名冲突会抛异常，需修复） |

---

## 7. 数据与协议模型（增量）

### 7.1 节点间 WS 消息（在现有 `PeerWireMessage` 上扩展）

```
{t:'hello', accountId, nodeId, name}          # 握手（已有；accountId=账号，nodeId=设备）
{t:'msg', id, from, fromName, to, body, ts}   # 消息（已有；from/to 为 accountId）
{t:'ack', id}                                 # 送达确认（新增）
{t:'file', id, name, size, mime, sha256}      # 文件元数据（新增，P1）
{t:'chunk', id, seq, data}                    # 文件分片（新增，P1）
{t:'recall', id}                              # 撤回（新增，P2）
{t:'edit', id, body, ts}                      # 编辑（新增，P2）
```

### 7.2 持久化

```
~/.dsh/plugins-data/lan-im/
├── config.json          # {nodeId, account:{username, passwordHash, displayName}, pskHash?}
├── history/             # 按天分文件（替代 history.jsonl）
│   └── 2026-08-16.jsonl
├── outbox/              # 离线待发队列（按目标 nodeId）
│   └── <nodeId>.jsonl
└── files/               # 接收的文件
```

---

## 8. 路线图与优先级

| 阶段 | 内容 | 里程碑口径 |
|---|---|---|
| **P0** | 5.9 账号体系 + 5.1 消息可靠 + 5.2 通知未读 + 5.3 发现兜底 | 「有账号身份、消息不丢、来消息知道、什么网络都能发现」 |
| **P1** | 5.4 文件传输 + 5.5 检索历史 | 「能传文件、历史可查可搜」 |
| **P2** | 5.6 安全 + 5.7 多频道 + 5.8 消息操作 | 「可信、可治理、可协作」 |

---

## 9. 风险与依赖

| 风险 | 影响 | 缓解 |
|---|---|---|
| mDNS 组播被隔离 | 发现失败 | 5.3 双通道兜底 |
| 消息丢失 | 信任受损 | 5.1 ack + outbox |
| 开放信任/明文 | 信息泄露 | 5.6 PSK + 加密 |
| 节点身份冲突崩溃 | 稳定性 | 修复 mDNS 服务名冲突的未捕获异常 |
| 跨版本不兼容 | 升级困难 | 协议带版本号，节点标识/历史格式向后兼容 |

---

## 10. 成功指标（可观测）

- **投递可靠率**：局域网内正常在线节点的消息送达率 = 100%（有 ack 可统计）。
- **离线补发成功率**：目标上线后 30s 内补发成功 ≥ 95%。
- **发现成功率**：含组播隔离场景，10s 内发现成功率 ≥ 95%。
- **通知触达**：窗口关闭时来消息，用户 5s 内感知率（通过点击通知/角标行为衡量）。
