# lan-im 下一步设计（v2）：频道、共享文件（局域网网盘）

> 状态：已实现（对应协议 v5 / e2e 覆盖）
> 基线：v0.4.0（账号、发现、可靠消息、多频道、文件、撤回编辑、搜索、PSK、未读通知、多端、按天历史、可缩放窗）

---

## 1. 频道功能设计

### 1.1 现状与问题

当前频道实现：
- 频道 `id` = 频道名（`Map<id, Channel>`，`id === name`）。
- 只有「公共频道 `*`」+ 命名频道；无重命名、无描述、无删除。
- 成员 = 所有在线节点（默认全员）；「离开」只是本地隐藏。
- 列表通过 `channelAdd` 广播 + hello/channelsSync 同步。

**问题**：
1. `id === name` 导致**无法重命名**（改名 = 新频道）。
2. 无**私有频道**，无法做「只给部分人看的频道」。
3. 无成员管理、无频道元信息。

### 1.2 目标模型

```
Channel {
  id: string          # UUID，稳定（重命名不变）
  name: string        # 显示名，可重命名
  description?: string
  kind: 'public' | 'private'
  createdBy: string   # 创建者 accountId
  createdAt: number
  members?: string[]  # 私有频道成员（含创建者）；公开频道省略
}
```

**两种频道**：
- **公开频道**：所有人可见、可加入、可发消息（等价于现在的命名频道）。
- **私有频道**：仅成员可见；创建者邀请成员；非成员看不到该频道、收不到消息。

### 1.3 操作与协议

| 操作 | 谁可做 | 协议（节点间广播） |
|---|---|---|
| 创建 | 任何人 | `channelAdd {channel}` |
| 重命名/改描述 | 创建者 | `channelUpdate {channel}` |
| 解散 | 创建者 | `channelDelete {id}` |
| 加入公开频道 | 任何人 | 本地订阅即可（频道已在列表） |
| 离开 | 任何人 | 本地移除（对公开频道=隐藏；对私有频道=退出成员，广播 `channelMemberRemove`） |
| 邀请（私有） | 创建者 | `channelMemberAdd {channelId, accountId}` |
| 移除（私有） | 创建者 | `channelMemberRemove {channelId, accountId}` |

**一致性模型**：频道元信息与成员列表由**创建者权威**（创建者广播变更，其余节点信任并应用）。这是去中心化下最简且可接受的做法。

### 1.4 消息路由

- 公开频道：发送给所有在线节点（现状）。
- 私有频道：只发送给 `members` 对应账号的节点；**接收端也校验**「我是否在 members」，不在则丢弃。

### 1.5 分阶段

- **P0**：`id` 改为 UUID（支持重命名）、频道元信息（描述/创建者/时间）、重命名、解散。
- **P1**：私有频道 + 邀请/移除成员 + 成员过滤路由。
- **兼容**：现有「公共频道 `*`」保留；老命名频道（id=name）升级迁移。

---

## 2. 共享文件设计（局域网网盘）

### 2.1 目标

每个频道一个**共享文件空间**，节点之间像「局域网网盘」一样浏览、上传、下载、直接使用文件。

### 2.2 存储模型

- 文件物理存储在**上传者（owner）节点**的磁盘（`files/` 目录）。
- 其他节点**按需拉取**：首次访问时从 owner 拉取并**本地缓存**，之后直接用缓存。
- 局限：owner 离线时只能使用已缓存文件（去中心化网盘的本质限制，接受）。

### 2.3 索引同步

- 文件元数据 `SharedFile` 广播 + hello 同步（与频道列表同机制），各节点维护一份索引。
- `SharedFile { id, name, size, mime, ownerNodeId, ownerAccount, channelId, uploadedAt, sha256 }`。

### 2.4 操作与协议

| 操作 | 协议 |
|---|---|
| 上传（本地文件） | owner 存储 + 广播 `fileAdd {file}` |
| 浏览 | 本地索引按频道过滤 |
| 下载/打开/使用 | 本地缓存命中直接返回；否则向 owner 发 `fileFetch {id}`，owner 走 WS 分片回传 `fileData` 并落缓存 |
| 删除（上传者） | 广播 `fileRemove {id}`，其余节点清索引 + 缓存 |

### 2.5 UI

频道顶部「消息 / 文件」两个 tab；「文件」tab 显示该频道共享文件列表 + 上传按钮 + 下载/打开/删除。图片内联预览。

---

## 3. 依赖与风险

| 依赖/风险 | 说明 | 缓解 |
|---|---|---|
| 私有频道一致性 | 去中心化下成员列表如何一致 | 创建者权威 + 广播，接受最终一致 |
| 频道 `id` 迁移 | 老频道 id=name 要迁移到 UUID | 启动时一次性迁移，保留 name 映射 |
| 网盘索引规模 | 文件多时列表性能 | 分页/懒加载 |
| owner 离线 | 未缓存文件无法拉取 | 按需拉取 + 本地缓存，接受此限制 |

---

## 4. 建议优先级

1. **频道 P0**（UUID + 重命名 + 解散）——改动小、补明显短板。
2. **网盘（共享文件空间）**——频道文件空间 + 上传/下载/按需拉取，价值直接。
3. 频道 P1（私有频道）——后续迭代。
